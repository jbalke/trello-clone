import { createTheme } from 'theme-in-css';

export const Theme = createTheme({
  color: {
    primary: '#3179ba',
    primaryLight: '',
    primaryDark: '',
    black: '#222',
    white: '#eee',
    grey: '',
  },
});
